#include <string>
#include <map>
#include <stack>
#include <queue>
#include <vector>
#include <algorithm>
#include <cstring>
#include <iostream>
#include <sstream>
using namespace std;
#pragma warning (disable:4996)
int main() {
	int n;
	cin >> n;
	for (int i = 0; i < n; i++) {
		string str;
		stack<char> s;
		cin >> str;
		s.push(str);
		
		for (int j = 0; j < str.size(); j++) {
					
		}
	}
}